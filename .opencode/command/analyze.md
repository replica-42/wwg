---
description: Analyze user behavior and generate profile
agent: build
---

Load user-behavior-analysis skill, analyze user's profile for period $1 (format: YYYY-MM). **YOU MUST save result as JSON** to preferences directory named `yyyy-MM.json`. **Using Simplified Chinese for output result**. **CRITICAL: Always verify the JSON file is actually created and saved - do not just output the JSON content without saving it to the file.**

